---
:backends:
  - yaml
:yaml:
  :datadir: /etc/puppet/environments/%{environment}/hiera
:hierarchy:
  - secrets/nodes/%{::fqdn}
  - secrets/level/%{::machine_level}
  - secrets/secrets
  - machine_region/%{::machine_region}
  - "hosts/%{::fqdn}"
  - roles/%{::role}
  - common
