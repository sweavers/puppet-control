---
:backends:
  - yaml
:yaml:
  :datadir: /etc/puppet/environments/%{environment}/hiera
:hierarchy:
  - secrets/nodes/%{::fqdn}
  - secrets/level/%{::machine_level}
  - secrets/secrets
  - level/%{::machine_level}
  - "hosts/%{::fqdn}"
  - roles/%{::machine_role}
  - common
