---
:backends:
  - yaml
:yaml:
  :datadir: /etc/puppet/environments/%{environment}/hiera
:hierarchy:
  - secrets/nodes/%{::fqdn}
  - secrets/machine_region/%{::machine_region}
  - secrets/secrets
  - machine_region/%{::machine_region}
  - "hosts/%{::fqdn}"
  - roles/%{::role}
  - common
